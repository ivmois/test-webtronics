import Arrow from "../steps-list/steps-card/arrow";
import styles from "./steps-line.module.sass";

const StepsLine = () => {
  return (
    <div className={styles.line}>
    </div>
  );
};

export default StepsLine;
