export enum NavMenuType {
  header = "header",
  footer = "footer",
}

export enum ButtonType {
  button = "button",
  submit = "submit",
}

export enum PositionItem {
  left = "left",
  right = "right",
}
